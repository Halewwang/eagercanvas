{
  "baseUrl": "${PERF_BASE_URL}",
  "concurrency": 10,
  "requests": 60,
  "bearerToken": "${PERF_BEARER_TOKEN}",
  "requestsList": [
    {
      "method": "GET",
      "path": "/projects"
    },
    {
      "method": "GET",
      "path": "/projects/${PERF_PROJECT_ID}"
    }
  ]
}
