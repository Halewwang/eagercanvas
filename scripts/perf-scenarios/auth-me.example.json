{
  "baseUrl": "${PERF_BASE_URL}",
  "concurrency": 10,
  "requests": 50,
  "bearerToken": "${PERF_BEARER_TOKEN}",
  "requestsList": [
    {
      "method": "GET",
      "path": "/auth/me"
    }
  ]
}
